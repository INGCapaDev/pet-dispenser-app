import { useContext } from 'react';
import { CountdownContext } from '../../context/CountdownContext.jsx';

const Config = () => {
  const { updateValues } = useContext(CountdownContext);

  return <></>;
};
export default Config;
